{
  "name": "@terms-and-conditions-analyzer/extension",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --environment jsdom"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.30",
    "jsdom": "^27.2.0",
    "typescript": "^5.9.3",
    "esbuild": "^0.27.0",
    "vitest": "^4.0.7"
  }
}
